@model IEnumerable<Student>


<h1>bonjour depuis /Student/Index</h1>
<p>Voici les informations de l'etudiant:</p>
<ul>
    @foreach (var student in Model)
    {
        <li>@student.Firstname @student.Lastname</li>
    }
</ul>